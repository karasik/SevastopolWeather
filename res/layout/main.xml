<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginBottom="@dimen/widget_margin_vert"
    android:layout_marginLeft="@dimen/widget_margin_hor"
    android:layout_marginRight="@dimen/widget_margin_hor"
    android:layout_marginTop="@dimen/widget_margin_vert"
    android:background="#44444466"
    android:orientation="horizontal" >

    <TextView
        android:id="@id/temperature"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:alpha="1.0"
        android:fontFamily="Verdana"
        android:gravity="center"
        android:shadowColor="#000000"
        android:shadowRadius="10"
        android:textSize="14pt" />

</LinearLayout>